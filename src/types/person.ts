export class Person {
    constructor(
        readonly name: string,
        readonly cpf: string,
        readonly birthDate: string,
        readonly driverCode: string,
        readonly phone: string
    ) { }
}